// Unused scratch file - safe to delete. It was a one-off read-only
// diagnostic script; it made no writes to your database. Feel free to
// remove it from Finder (the tools I have can edit its contents but
// can't delete/rename files already written into this project folder).
